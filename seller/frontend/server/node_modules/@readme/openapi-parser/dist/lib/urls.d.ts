/**
 * Determine if a given URL is unsafe.
 *
 * An unsafe URL is one that is not publicly accessible (no private IPs, private ports, etc.)
 *
 */
declare function isUnsafeURL(url: string): boolean;

export { isUnsafeURL };
